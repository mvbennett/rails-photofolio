<div class="container">
  <%= simple_form_for(@photograph) do |f| %>
    <%= f.input :title, label: 'Title (totally optional)', maxlength: 30 %>
    <%= f.input :photo, as: :file %>
    <%= f.button :submit, 'Upload Photo', class: 'btn-primary' %>
  <% end %>
</div>
